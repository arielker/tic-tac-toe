server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: proxy API requests to backend (if you prefer nginx to route /api)
  # location /api/ {
  #   proxy_pass http://server:4000/; 
  # }
}
